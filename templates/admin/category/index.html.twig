{% extends 'base.html.twig' %}

{% block title %}Category index {% endblock %}

{% block body %}
<h1>Category index Admin</h1>

<a href="{{ path('admin_category_new') }}">Create new</a>

<table class="table">
    <tbody>
        {% for category in categories %}
        <tr>
            <td>
                <a href="{{ path('admin_category_show', {'category': category.id}) }}">{{ category.name }}</a>
              
                <a href="{{ path('admin_category_edit', {'id': category.id}) }}">edit</a>


            </td>
          
        </tr>
        {% else %}
        <tr>
            <td colspan="3">Aucune catégorie</td>
        </tr>
        {% endfor %}
    </tbody>
</table>

{% endblock %}