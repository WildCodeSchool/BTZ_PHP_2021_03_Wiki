{% extends 'base.html.twig' %}

{% block title %}Thème index{% endblock %}

{% block body %}

<ol class="breadcrumb">
    <li class="breadcrumb-item">
        <a href="{{ path('home') }}">Accueil</a>
    </li>
    <li class="breadcrumb-item">
        <a href="{{ path('category_index')}}">Thèmes</a>
    </li>
    <li class="breadcrumb-item active">Edition</li>
</ol>
<h1>Edition du thème : </h1>

{# Admin only #}
{% if is_granted('ROLE_ADMIN') %}

{{ include('category/_form.html.twig', {'button_label': 'sauve'}) }}
<a href="{{ path('category_index') }}">retour</a>

{# {{ include('category/_delete_form.html.twig') }} #}

{% else %}
<a>Permission refusée, contacter l'administrateur</a>
{% endif %}

{% endblock %}