{% extends 'base.html.twig' %}

{% block title %}Catégorie - Détails{% endblock %}

{% block body %}
<h1>Categorie - Détails</h1>

<table class="table">
    <td> # {{ category.id }} - {{ category.name }}</td>
    <tbody>
      
        <tr>
            {% for article in category.articles %}
            <div>
                <p>{{article.title}}</p>
                <!-- LI add here all needed article fields such as date, .. -->
            </div>>
            {% else %}

            <li>Aucun article dans cette catégorie</li>
            {% endfor %}

        </tr>
    </tbody>
</table>

<a href="{{ path('category_index') }}">back to list</a>

<a href="{{ path('category_edit', {'category': category.id}) }}">edit</a>

{{ include('category/_delete_form.html.twig') }}
{% endblock %}