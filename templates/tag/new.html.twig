{% extends 'base.html.twig' %}

{% block title %}Création d'un mot clé
{% endblock %}

{% block body %}
	<ol class="breadcrumb">
		<li class="breadcrumb-item">
			<a href="{{ path('home') }}">Accueil</a>
		</li>
		<li class="breadcrumb-item">
			<a href="{{ path('tag_index')}}">Mots-clés</a>
		</li>
		<li class="breadcrumb-item active">Création</li>
	</ol>

	<h1>Création d'un mot clé</h1>

	{# Admin only #}
	{% if is_granted('ROLE_ADMIN') %}
		{{ include('tag/_form.html.twig') }}
		<a href="{{ path('tag_index') }}">retour</a>

	{% else %}
		<a>Permission refusée, contacter l'administrateur</a>
	{% endif %}


{% endblock %}
