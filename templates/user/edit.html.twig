{% extends 'base.html.twig' %}

{% block title %}Edition d'utilisateur
{% endblock %}

{% block body %}

	<!-- Page Heading/Breadcrumbs-->
	<ol class="breadcrumb">
		<li class="breadcrumb-item">
			<a href="{{ path('home') }}">Accueil</a>
		</li>
		<li class="breadcrumb-item">
			<a href="{{ path('user_index') }}">Utilisateurs</a>
		</li>
		<li class="breadcrumb-item active">Edition</li>
	</ol>

	<h1>Edition d'utilisateur</h1>
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn btn-primary">{{ button_label|default('Mettre à jour') }}</button>
	{{ form_end(form) }}

	{{ include('user/_delete_form.html.twig') }}

    <div class="pb-4 pt-4">
    <a href="{{ path('user_index') }}"><i class="fas fa-arrow-circle-left mr-2"></i>Retour</a>
    </div>

{% endblock %}
