{% extends 'base.html.twig' %}

{% block title %}Liste des utilisateurs{% endblock %}

{% block body %}
    <h1>Liste des utilisateurs</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Ville</th>
                <th>Valider</th>
            </tr>
        </thead>
        <tbody>

        {% for user in users %}

            <tr>
                <td>{{ user.lastname }}</td>
                <td>{{ user.firstname }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                <td>{{ user.cityAgency }}</td>
                <td>
                    {{ form_start(forms[user.id]) }}
                    {{ form_widget(forms[user.id]) }}
                    <button class="btn" name="id" value={{user.id}}>{{ button_label|default('Enregistrer') }}</button>
                    {{ form_end(forms[user.id]) }}
                </td>
                {# <td><a href="{{ path('user_edit', {'id': user.id}) }}">Valider</a></td> #}





                {# {% if user.validated is same as(true) %}
                <td>{{ "validé" }}</td>
                {% else %}
                <td>{{ "en attente" }}</td>                                
                {% endif %}
                 #}
             
                {# <td>
                    <a href="{{ path('user_edit', {'id': user.id}) }}">Modifier</a>
                    <a href="{{ path('user_delete', {'id': user.id}) }}">Supprimer</a>
                    {% include "user/_delete_form.html.twig" %}
                </td> #}
            </tr>
        {# {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr> #}
        {% endfor %}
        </tbody>
    </table>

    {# <a href="{{ path('user_new') }}">Create new</a> #}
{% endblock %}
